#include <stdio.h>

/**
* main - prints first half of sentence
* Return: 0 if no error
*/
int main(void)
{
	char string[] = "\"Programming is like building a multilingual puzzle";

	puts(string);

	return (0);
}
